server:
  port: 14001
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: auth-service
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    sender:
      type: kafka
    kafka:
      topic: zipkin
  kafka:
    bootstrap-servers: 127.0.0.1:9092